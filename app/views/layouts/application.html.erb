<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Payrollsio</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/messages' %>
    <%= yield %>
    
    <script type="text/javascript">
      <% if signed_in? -%>
        Payrollsio.currentUser = new Payrollsio.Entities.User(<%= current_user.to_json.html_safe %>);
      <% else -%>
        Payrollsio.currentUser = null;
      <% end -%>
    </script>

  </body>
</html>